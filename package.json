{
  "name": "regarde-il-neige-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Version back-end du site Regarde il neige.",
  "main": "server/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "audit:static": "node server/scripts/audit-static.mjs",
    "verify": "npm run audit:static && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "multer": "^2.2.0",
    "sharp": "^0.35.1"
  }
}
